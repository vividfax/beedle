class Forest {

    constructor(x, y) {

        this.edgePadding = 100;
        this.x = x;
        this.y = y;

        this.radius = 30;
    }

    update() {

    }

    display() {

        // noStroke();
        // fill(200, 100);
        // ellipse(this.x, this.y, this.radius);
    }
}