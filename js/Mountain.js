class Mountain {

    constructor(x, y) {

        this.edgePadding = 100;
        this.x = x;
        this.y = y;

        this.radius = random(50, 100);
    }

    update() {

    }

    display() {

        // noStroke();
        // fill("#aaa");
        // ellipse(this.x, this.y, this.radius);
    }
}