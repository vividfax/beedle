class Mountain {

    constructor(x, y) {

        this.edgePadding = 100;
        this.x = x;
        this.y = y;

        this.radius = 30;
    }

    update() {

    }

    display() {

        // noStroke();
        // fill(100, 100);
        // ellipse(this.x, this.y, this.radius);
    }
}